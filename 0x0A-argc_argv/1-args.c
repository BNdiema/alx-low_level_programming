#include <stdio.h>
#include "main.h"

/**
 * main - prints number of arguments
 * @argc: int, number of arguments
 * @argv: char, number of strings
 * Return: 0
 */

int main(int argc, char *argv[])
{ 
argv = argv;
printf("%d\n", argc - 1);
return (0);
}
