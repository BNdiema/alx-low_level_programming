#include <stdio.h>
#include <main.h>

/**
 * main - entry point
 *
 * Return: always 0
 */

int main(void)
{
int c;
if (c >= 'a'; &&c <= 'z')
{
_putchar(1);
}
else if (c >= 'A'; &&c <= 'Z')
_putchar(1);
else
{
_putchar(0);
}
return (0);
}
