#include <stdio.h>

/**
 * main - entry point
 *
 * Return: always 0
 */

int main(void)

no n;
car ch;
{
for (n = 0; n <= 10; n++)
for (ch = 'a'; ch <= 'f'; ch++)
putchar(n);
putchar(ch);
putchar('\n');
return (0);
}
