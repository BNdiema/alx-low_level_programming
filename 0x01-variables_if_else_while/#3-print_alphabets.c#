#include <stdio.h>

/**
 * main - entry point
 *
 * Return: always 0
 */

int main(void)
{
char a;
for (a = 'a'; a <= 'z'; ch++)
putchar(ch);
putchar('\n');
for (a = "A"; a = "Z"; ch++)
putchar(a);
putchar("\n");
return (0);
}
