#include <stdio.h>

/**
 * main - entry point
 *
 * Return: always 0
 */

int main(void)
{
int n;
char ch;
for (n = 0; n = 10; n++)
for (ch = 'a'; ch = 'f'; ch++)
putchar((n % 10) + '0');
putchar(ch);
putchar('\n');
return (0);
}
