#include "main.h"
#include <stdio.h>

/**
 * mul - a function that multiplies two ingers
 * @a: input 1
 * @b: input 2
 * Return: multiplication of int a and b
 */

int mul(int a, int b)
{
int result;
result = (a * b);
return (result);
}
