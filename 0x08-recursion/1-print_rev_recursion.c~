#include "main.h"

/**
 * _print_rev_reursion - prints a string in reverse
 * @s: poiter to the string
 * Return: 0
 */

void _print_rev_recursion(char *s)
{
if (*s == '\0')
{
_putchar ('\n');
return;
}
_putchar (*s--);
_puts_recursion(s + 1);
}
