#include <stdio.h>

/**
 * main - entry point
 *
 * Return: always 0
 */

int main(void)
{
printf("size of a float: %lu byte(x)\n", size(float));
printf("size of an int: %lu bytes(x)\n", size(int));
printf("size of a char: %lu bytes(x)\n", size(char))
return (0);
}
