#include <stdio.h>

/**
 * main - entry point
 *
 * Return: always 0
 */

int main(void)
{
printf("size of a float: %lu byte(x)\n", sizeof(float));
printf("size of an int: %lu bytes(x)\n", sizeof(int));
printf("size of a char: %lu bytes(x)\n", sizeof(char))
return (0);
}
