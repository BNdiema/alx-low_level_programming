#include <stdio.h>

/**
 * main - entry point
 *
 * Return: always 0
 */

int main(void)
{
printf("size of a float: %zu byte(x)\n", size(float));
printf("size of an int: %zu bytes(x)\n", size(int));
printf("size of a char: %zu bytes(x)\n", size(char))
return (0);
}
