#include <stdio.h>

/**
 * main - entry point
 *
 * Return: always 0
 */

int main(void)
{
printf("size of a float: %d byte(x)\n", sizeof(float));
printf("size of an int: %d bytes(x)\n", sizeof(int));
printf("size of a char: %d bytes(x)\n", sizeof(char))
return (0);
}
