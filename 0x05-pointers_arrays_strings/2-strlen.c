#include "main.h"
#include <stdio.h>

/**
 * _strlen - retunrns the lenth of a string
 * @s: string
 * Return: lenth of string
 */

int _strlen(char *s)
{
char l;
for (*s = 0; l[*s] != '\0'; ++*s)
_putchar(*s);
return (0);
}
